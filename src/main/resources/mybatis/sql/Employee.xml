<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.wizard.pracmybatis.repository.EmployeeRepository">

    <select id="getList" resultType="Employee">
        SELECT E.id,
               E.name,
               E.address,
               E.company_id
        FROM employee E
        ORDER BY E.id
    </select>

    <select id="get" parameterType="int"
            resultType="Employee">
        SELECT E.id,
               E.name,
               E.address,
               E.company_id
        FROM employee E
        WHERE E.ID = #{e_id}
    </select>

    <insert id="save" parameterType="Employee">
        INSERT INTO employee (name, address, company_id)
        VALUES (#{name}, #{address}, #{company_id})
    </insert>

    <update id="update" parameterType="Employee">
        UPDATE employee
        SET name = #{name},
            address       = #{address},
            company_id = #{company_id}
        WHERE id = #{e_id}
    </update>

    <delete id="delete" parameterType="int">
        DELETE
        FROM employee
        WHERE ID = #{e_id}
    </delete>

    <select id="getEmployeeCompanyList" resultType="EmployeeCompanyDto">
        SELECT E.name,
               E.address,
               C.id,
               C.name,
               C.address
        FROM employee E,
             company C
        WHERE E.company_id = C.id
    </select>
</mapper>